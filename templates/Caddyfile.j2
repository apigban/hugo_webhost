{% for domain in sites %}
{{ ansible_host }}:{{ domain.port }} {
    root * {{ web_path }}/{{ domain.name }}

    # Enable the static file server.
    file_server
    
    # Enable logging
    log {
        output file /var/log/caddy/access-{{ domain.name }}.log
    }
}
{% endfor %}